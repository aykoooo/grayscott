# GPU Optimization Worker v3 — nabla-type-lite Engine

You are optimizing a Gray-Scott reaction-diffusion engine's WGSL compute shaders
for use in nabla-type-lite, a browser WebGPU playground. Target: maximum cells/sec
at arbitrary resolutions (nothing hardcoded).

This file IS your brain. Every session starts fresh; use files for state.

---

## 🚀 RAPID START (do this FIRST)

Read these files to snapshot current state:

```
NABLA_PLAN.md    — what needs doing? First unchecked [ ] is today's task
PERFORMANCE.md   — how fast is current best?
KNOWLEDGE.md     — what have we learned?
```

Then answer: **what is today's task**, based on the first unchecked `[ ]` in NABLA_PLAN.md?

---

## 🔄 MODE AWARENESS

| Mode | Signal | Behavior |
|---|---|---|
| **Manual** (human invoked `/loop`) | No orchestrator | Work through ALL remaining [` `] tasks sequentially. Self-mark `[x]` and commit each. Output `<promise>DONE</promise>` only when NABLA_PLAN.md has no more `[ ]` |

---

## 🎯 TARGET: Browser WebGPU via Pre-Compiled WASM

nabla-type-lite loads `gray_scott_shader.wasm` as a static asset. The WASM module exports:
- `gs_wasm_build_periodic(w, h, tx, ty)` → returns optimized WGSL string + metadata
- `gs_wasm_meta(w, h, tx, ty)` → dispatch counts, workgroup sizes, buffer layouts

The shader must handle ANY W×H (playground allows arbitrary resolution).
We target Chrome 134+ for subgroups; Firefox/Safari get standard tiling fallback.

---

## 📋 MANDATORY WORKFLOW (per task)

### 0. Quick assessment
Confirm which phase.task you're tackling from NABLA_PLAN.md.

### 1. Research (for NEW techniques — skip for docs/config/fixes)
- Web search `ddg_search_web_search` the technique + "WGSL subgroup shuffle" / "WebGPU compute"
- Fetch 2-3 articles via `ddg_search_web_fetch_content`
- Write summary in `RESEARCH_NOTES.md`

### 2. Implement
Editable files:
- `src/wgsl_gen.zig` — All generateWgsl* variants (standard, subgroups, coarse, temporal)
- `src/wasm_shader.zig` — WASM exports, feature detection, engine selection
- `src/gpu/gpu.zig` — Native pipeline (delegates to wgsl_gen), benchmark driver
- `build.zig` — Build targets for wasm-shader, tests, benchmarks

For each new WGSL variant, add a function to `wgsl_gen.zig` and export it via `wasm_shader.zig`.

Build: `zig build` + `zig build wasm-shader`

### 3. Test (non-negotiable)
```
zig build test
```
Must pass. If it fails, fix code. Do NOT proceed.

Also verify: `zig build wasm-shader` produces `zig-out/bin/gray_scott_shader.wasm`.

### 4. Benchmark (non-negotiable, NATIVE only)
The native GPU benchmark verifies correctness + performance:
Run 3 times:
```
zig build bench-gpu 2>&1
```
Take MEDIAN `cells_per_second`. Output format:
`{"cells_per_second":NNN,"hash":"HEX","width":256,"height":256,"steps":500}`

Browser benchmarks can only be done manually in Chrome DevTools — note when native
throughput indicates a clear win (>10%) so the user knows to test in-browser.

### 5. Verify correctness — SACRED HASH
- Hash MUST be exactly: `e16ed0e3c29cc50b5fa2b42791f31ab00b39d488e971b5d3c6017970ed037a43`
- If hash differs → code broke determinism → `git reset --hard HEAD` immediately
- Do NOT debug broken hashes; revert and try a different approach

**Important for subgroups**: subgroup operations change data flow. You MUST verify
that every thread computes using IDENTICAL arithmetic regardless of whether values
come from SMEM or subgroup ops. The subgroup variant is just a transport mechanism —
the math (Laplacian coefficients, Gray-Scott formula) stays identical.

### 6. Compare to baseline
Find current best in `PERFORMANCE.md`.

| Outcome | Action |
|---|---|
| Median > best + 10% | KEEP IT. Record in PERFORMANCE.md, append to KNOWLEDGE.md, mark `[x]` in NABLA_PLAN.md, commit. Advance to next task. |
| Slower / same | REVERT. `git reset --hard HEAD`. Log in PERFORMANCE.md as "Reverted — slower". Try DIFFERENT approach. |
| Hash mismatch | REVERT. `git reset --hard HEAD`. Same as above. |

---

## 🛑 STALL PREVENTION

| Attempt # | Rule |
|---|---|
| 1 | Try the standard approach |
| 2 | Try an alternative implementation |
| 3 | Last chance — try creative/simpler variant |
| 4+ | ESCALATE: Mark `[BLOCKED: reason]` in NABLA_PLAN.md, write to KNOWLEDGE.md, move to next task. |

Per-task counter. Hit attempt 4 = block it and advance.

---

## ⚡ TASK BATCHING

| Weight | Examples | Batch size |
|---|---|---|
| FAST (< 3 min) | Doc updates, config changes | 3-5 per session |
| MEDIUM (5-20 min) | Implementation + test + bench | 1 per session |
| HEAVY (> 20 min) | New WGSL variant + full sweep | 1 per session |

---

## 🛠️ Tools

### Web Research
- `ddg_search_web_search` — WGSL/subgroup/stencil optimization
- `ddg_search_web_fetch_content` — Read full articles

### Codebase
- `read`, `grep`, `glob` — Explore files
- `lsp` — Find definitions/references

### Build & Test
- `zig build` — Compile native CLI
- `zig build wasm-shader` — Build gray_scott_shader.wasm
- `zig build test` — Run Zig unit tests
- `zig build bench-gpu` — GPU benchmark (256²/500)
- `zig build bench-gpu-512` — GPU at 512²/500
- `zig build bench-gpu-1024` — GPU at 1024²/100
- `zig build bench-map` — Full pipeline (256²/5000)
- `zig build bench-map-512` — Pipeline at 512²/5000
- `zig build bench-map-1024` — Pipeline at 1024²/1000

---

## 📐 Key Technical Details

### Current Architecture
- **Workgroup**: 16×4 (64 threads) — reshaped for warp locality
- **STRIDE**: TX+2 = 18 (tile width includes 1-cell halo each side)
- **Tile**: 18×6 shared memory arrays for u/v
- **Command buffer batching**: All dispatches in one submit — DONE, sacred
- **Ping-pong**: U0↔U1, V0↔V1 with two bind groups
- **Periodic boundaries**: `select()` for both tile loading and Laplacian
- **Pearson variant**: Neumann boundaries (`max`/`min`) + spatial feed/kill maps
- **Hash**: `e16ed0e3c29cc50b5fa2b42791f31ab00b39d488e971b5d3c6017970ed037a43` — sacred

### Performance
- Peak (native RTX 4060): 2.35B cells/sec (8×8 tiling + batched dispatch)
- Recent best: ~681M cells/sec (16×4 reshape, on thermally-degraded session)
- Peak (step-only at 1024²): ~4.2B cells/sec
- CPU reference: ~500M cells/sec (single-threaded ReleaseFast)
- Theoretical BW ceiling: 13.25B cells/sec at 272 GB/s
- Current utilization: 17.7% of bandwidth ceiling

### Browser Subgroup Layout (16×4)
```
Warp 0: threads 0..31 = rows 0 and 1 (both 16-wide)
Warp 1: threads 32..63 = rows 2 and 3
```
- Thread (row y, col x): lane_id = y*16 + x within warp half
- Horizontal ±1 neighbors: lanes ±1 in same 16-thread row
- Vertical ±1 row neighbor: lanes ±16 (within same 64-thread warp across 2 rows)
- **All 8 stencil neighbors accessible via subgroup shuffles within single warp**

### Dual-Path Browser Strategy
| Browser | Features | Shader variant |
|---|---|---|
| Chrome 134+ | subgroups, shader-f16 | generateWgslSubgroups() |
| Firefox/Safari | none yet | generateWgsl() (standard tiling) |

Feature detection happens in JS-land (`adapter.features.has('subgroups')`),
passed to WASM module which returns appropriate variant.

### File Map
| File | Editable? | Purpose |
|---|---|---|
| `src/wgsl_gen.zig` | ✅ YES | All WGSL generation functions |
| `src/wasm_shader.zig` | ✅ YES | WASM exports, engine selection |
| `src/gpu/gpu.zig` | ✅ careful | Delegates to wgsl_gen for actual templates |
| `build.zig` | ✅ YES | Build targets, modules |
| `src/simulation.zig` | ❌ NEVER | CPU reference, sacred |
| `src/grid.zig` | ❌ NEVER | Grid structure, sacred |
| `test/` | ❌ NEVER | Test infrastructure |

---

## 🔬 Phase Reference (from NABLA_PLAN.md)

| Phase | Goal | Key deliverable |
|---|---|---|
| **0** Fix Naga blocker | Upgrade wgpu-native so subgroups compile natively | bench-gpu-subgroups works |
| **1** Dynamic sizing + integration | Auto tile_x/tile_y for any W×H, bind group export | wasm exports usable by nabla-type-lite |
| **2** Subgroup shuffle | Replace SMEM reads with intra-warp register shuffles | generateWgslSubgroups() |
| **3** Thread coarsening | 2 cells/thread, halved dispatches, combined subgroups variant | generateWgslCoarse() + generateWgslCoarseSubgroups() |
| **4** Temporal blocking | 2-step fusion via subgroup intermediate propagation | generateWgslTemporal() |
| **5** Dynamic engine selection | Auto-select best variant + workgroup shape per resolution + browser capabilities | gs_wasm_get_best() smart selector |

---

## 🚫 NEVER MODIFY

- `src/simulation.zig`, `src/grid.zig` — CPU reference, sacred correctness
- `src/map.zig`, `src/main.zig`, `src/wasm.zig` — application code
- `BENCHMARK/reference_hashes.txt` — sacred reference hashes
- `test/` — test infrastructure

---

## 🧪 Already Explored & Exhausted

- **f16 storage**: Zero gain, compute-bound at tested scales
- **vec2 UV packing**: -38%, register pressure > coalescing benefit
- **Bank conflict padding**: <5% impact at 8×8, measured below noise floor
- **Temporal blocking (naive)**: Requires coarsening groundwork, complex on 64 threads
- **Diamond/wavefront tiling**: Overkill for 9pt 2D stencil
- **Tensor cores**: FP32 scalar stencil doesn't map to matrix multiply
- **Semi-stencil**: Only benefits r≥3 (ours is r=1)
- **Multi-GPU / async copy / persistent threads**: Not applicable in wgpu/browser

---

## ✅ Completion Signal

| When | Output |
|---|---|
| Current task finished + committed, more exist | Continue to next task. Do NOT output completion signal. |
| Hit `[BLOCKED]` phase (e.g. Naga subgroups) | Mark blocked, write to KNOWLEDGE.md, skip to next non-blocked phase |
| All NABLA_PLAN.md tasks are `[x]` or `[BLOCKED]` | `<promise>DONE</promise>` |
| Stuck after escalation / manual exit | `<promise>DONE</promise>` with explanation |

If Phase 0 is blocked, skip directly to Phase 3.1 (coarse no subgroups) and Phase 5
(dynamic selection) — these don't need subgroup execution.

---

## 🗜️ Compactions

The loop generates many small atomic commits. After every 5-10 commits or when a
phase completes, git accumulates loose objects. Compact them:

```
git gc --aggressive    # compact loose objects into packs, reclaim disk space
```

Squash per-phase commits at phase boundaries:

```
# After all Phase N tasks are done:
git rebase -i HEAD~N   # squash Phase N's N commits into one
```

**When to compact:**
- End of each phase (all sub-tasks [x] for that phase)
- Before starting a new ocloop session (cleaner history = faster reset)
- Every ~20 total commits (loose object count exceeds ~100)

**Agent rule:** After completing the LAST task in any phase, output:
`⚡ Phase N complete. Run: git gc --aggressive to compact.`

Do NOT auto-squash or auto-gc — let the human decide.

---

## 🔄 Recovery

```
git reflog --oneline          # find good commit
git reset --hard <good-hash>  # restore
```
Files in `.gitignore` (like `vendor/`) survive resets.

---

**BEGIN WORK. Rapid-start: read NABLA_PLAN.md, PERFORMANCE.md, KNOWLEDGE.md. Identify first unchecked task. Execute.**