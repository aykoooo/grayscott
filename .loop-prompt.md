# GPU Optimization Worker

You are optimizing a Gray-Scott reaction-diffusion engine's GPU compute shader. You work on ONE task at a time from PLAN.md.

## 🛠️ Your Tools — USE THEM

You have access to powerful research and computation tools. USE them for every research task. The key to beating this benchmark is RESEARCH before implementation.

### Web Research (use first on every research task)
- **Web search** — Search the live internet for "WGSL shared memory tiling", "WebGPU compute shader optimization", "stencil GPU shared memory", etc.
- **Fetch URL** — After finding a promising article, fetch and read it fully.
- **GitHub search** — Use bash: `gh search repos "WebGPU stencil"` or `gh search code "workgroup_size" --language=glsl`.

### Math & Computation (use when analyzing stencil coefficients, tile sizes, memory bandwidth)
- **Solver** — Use for calculating theoretical speedups: "If shared memory saves 16 f32 loads per thread per step, and bandwidth is X GB/s, what is the acceleration?"
- **Plot** — Visualize if helpful.

### Documentation Search (for WebGPU API specifics)
- **DocFork** — Search the WebGPU spec or wgpu-native docs: `DocFork_docfork_search_docs` with query about `wgpuCommandEncoderBeginComputePass` or WGSL `workgroupBarrier`.

### Codebase Exploration (always start here)
- **Read files** — Read the current `src/gpu/gpu.zig` to understand the exact structure before modifying.
- **Grep** — Search for specific patterns: `grep -n "workgroup_size" src/gpu/gpu.zig`.
- **Benchmark** — `zig build bench-gpu`.

---

## 📋 Your Workflow (MANDATORY — do not skip steps)

### 1. Read current state
Before touching anything:
```bash
cat PLAN.md | grep "^- \[ \]" | head -5
cat PERFORMANCE.md | tail -20
cat KNOWLEDGE.md | tail -20
```

### 2. Identify the current task
Find the first `[ ]` (unchecked) item in PLAN.md.

### 3. RESEARCH (use tools!)
Before writing a single line of code:
- **Web search** the technique + "WGSL" or "WebGPU compute" or "GPU stencil"
- Read 2-3 sources. Fetch full articles if the abstract looks promising.
- Write a brief summary (2-3 sentences) in `RESEARCH_NOTES.md` — this persists across sessions.
- Note: WebGPU WGSL doesn't have CUDA-style `__shared__`; it uses `workgroup` storage class with `workgroupBarrier()`.

### 4. IMPLEMENT
- Edit `src/gpu/gpu.zig` (the shader is inside `generateWgsl()`)
- Build: `zig build`
- Fix any compile errors

### 5. TEST (non-negotiable)
```bash
zig build test
```
If it fails, fix the code. Do NOT proceed.

### 6. BENCHMARK (non-negotiable)
Run at least 3 times. The GPU has measurement noise.
```bash
for i in 1 2 3; do echo "=== Run $i ==="; zig build bench-gpu 2>&1 | grep -oP '\{.*\}'; done
```

The output looks like:
```json
{"cells_per_second":58537062,"hash":"e16ed0e3c29cc50b5fa2b42791f31ab00b39d488e971b5d3c6017970ed037a43"}
```

Take the **median** of the 3 `cells_per_second` values.

### 7. VERIFY CORRECTNESS
- Hash MUST be exactly: `e16ed0e3c29cc50b5fa2b42791f31ab00b39d488e971b5d3c6017970ed037a43`
- If hash differs: your code broke determinism. Run `git diff` to see what changed, fix it.
- If you can't fix it, `git reset --hard HEAD` and start the task over with a different approach.

### 8. COMPARE TO BASELINE
Read `PERFORMANCE.md` — find the current best.

- If median > best + 10%: **KEEP IT**
  - Record in `PERFORMANCE.md`
  - `git add -A && git commit -m "perf(gpu): <technique> (+X% to Y cells/sec)"`
  - Mark task `[x]` in `PLAN.md`
  - Append findings to `KNOWLEDGE.md`

- If slower / same / hash mismatch: **REVERT**
  - `git reset --hard HEAD` (restores to last known good)
  - Record the attempt in `PERFORMANCE.md` with "reverted — slower/hash mismatch"
  - **TRY A DIFFERENT APPROACH** — you may attempt multiple variants per task
  - Do NOT give up after one try unless you have 3 solid failed attempts

### 9. Signal completion
When the task is truly done (committed successfully or explicitly blocked after 3 honest tries):
```
<promise>DONE</promise>
```

---

## 📐 Key Technical Details

- **Shader is runtime-generated** — inside `generateWgsl()` in `src/gpu/gpu.zig`. The `.wgsl` file is NOT used.
- **Current workgroup**: 8×8 (64 threads) — parameterized via `wg_x`/`wg_y` in GpuState (line 14-15).
- **Shared memory tiling is DONE**: loads (TX+2)×(TY+2) tile into `var<workgroup>` arrays with edge/corner halo.
- **Command buffer batching is DONE**: `gs_gpu_steps(N)` records N dispatches in one command encoder, submits once.
- **Ping-pong buffers**: U0→U1 step 1, U1→U0 step 2. Two bind groups (even/odd) swap based on `current_u`.
- **Bounds handling**: Uses `select()` for periodic boundaries in both tile loading and computation.

## 📁 Audit Trail Files
- `PERFORMANCE.md` — benchmark results (YOU keep this up to date)
- `KNOWLEDGE.md` — what worked / what failed (YOU append findings)
- `RESEARCH_NOTES.md` — web research summaries
- `PLAN.md` — task checklist

## 🚫 NEVER MODIFY
- `src/simulation.zig`, `src/grid.zig` — CPU reference
- `BENCHMARK/reference_hashes.txt` — sacred reference

## ⚡ Remaining Targets (completed 1, 4, 5; try these next)
1. **f16 storage** — try actually querying `wgpuAdapterHasFeature(ShaderF16)` instead of assuming failure
2. **vec2<f32> UV packing** — combine u+v into one buffer read per cell (halves tile-load traffic even with f32)
3. **Multi-resolution benchmarks** — test at 512² and 1024² where bandwidth dominates (256² bench is ~14ms, too fast to measure GPU accurately)
4. **Map integration** — test with actual map generation (not benchmark loop), adaptive convergence for map mode
5. **Remove benchmark overhead** — SHA256 hashing steals CPU time inside the measurement window

## 🔄 Recovery
If `git reset --hard HEAD` goes too far (e.g., deletes your `vendor/` directory):
```bash
git reflog --oneline          # find a good commit
git reset --hard <good-hash>  # restore to it
```
The `vendor/` dir is in `.gitignore` and should survive most resets, but if it's gone, re-extract the `wgpu-native` release ZIP.

---

**BEGIN WORK NOW. Start with reading PLAN.md and identifying the first task.**
